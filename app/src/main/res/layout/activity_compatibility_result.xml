<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/main"
    android:background="@drawable/background">

    <!-- TextView for Compatibility Score -->
    <TextView
        android:id="@+id/tvCompatibilityScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Compatibility Score"
        android:textColor="@color/white"
        android:textSize="30sp"
        android:layout_marginTop="100dp"
        android:gravity="center"
        android:fontFamily="@font/boorsok"
        android:shadowColor="@color/black"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="2"
        android:layout_centerHorizontal="true"/>

    <!-- TextView for Compatibility Insights -->
    <TextView
        android:id="@+id/tvCompatibilityInsights"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Compatibility Insights"
        android:layout_below="@id/tvCompatibilityScore"
        android:textColor="@color/white"
        android:textSize="21sp"
        android:textStyle="bold"
        android:shadowColor="@color/black"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="2"
        android:layout_centerHorizontal="true"
        android:layout_margin="50dp"/>

    <!-- Button for Sharing -->
    <TextView
        android:id="@+id/btnShare"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/share"
        android:textColor="@color/white"
        android:textSize="30sp"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:fontFamily="@font/boorsok"
        android:shadowColor="@color/black"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="2"
        android:layout_below="@id/tvCompatibilityInsights"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
